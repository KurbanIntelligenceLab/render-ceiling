# Re-running the pipeline itself — rendering structures, computing labels, scoring the
# oracle — additionally needs the structure and imaging stack. Versions are unpinned
# because the pipeline was run over an extended period and no single set was used
# throughout; the released records and prediction vectors are the reproducible artifact,
# and the figures and verification gates in requirements.txt reproduce from them exactly.
-r requirements.txt

pymatgen
spglib
ase
Pillow
mp-api

# Model arms (fine-tuning and inference) additionally need:
# torch, transformers, peft, trl
# External baselines additionally need: dgl, alignn, jarvis-tools
